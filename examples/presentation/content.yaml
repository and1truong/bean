# This named content Block has explicit identity because the product thesis is
# reusable outside a single frame. Frame-local narrative lives inline in Panels.
kind: Block
name: product_statement
type: content
content:
  - {type: quote, text: "Model the application once. Let humans and agents operate the same validated system.", attribution: Bean}
  - {type: paragraph, text: "Probabilistic agents need a deterministic target with explicit semantics and repairable failures."}
  - {type: callout, tone: info, text: "Bean owns application semantics; providers own external infrastructure capabilities."}
