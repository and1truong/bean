import {test as base,expect} from './fixtures/bean';import {action,login} from './helpers/api';const test=base.extend<{}, {appName:string}>({appName:['commerce',{scope:'worker'}]});test('inventory conflict is deterministic',async({request,bean})=>{const csrf=await login(request,bean.baseURL);const p=await action(request,bean.baseURL,csrf,'product_create',{name:'Last item',price:100,inventory:1});const id=(await p.json()).data.id;expect((await action(request,bean.baseURL,csrf,'place_order',{product_id:id,quantity:1})).ok()).toBeTruthy();const second=await action(request,bean.baseURL,csrf,'place_order',{product_id:id,quantity:1});expect(second.status()).toBe(409)})
