export type Manifest={appId:string;releaseId:string;version:number;entities:Record<string,{Name:string;Label:string;Fields:Array<{Name:string;Label:string;Type:string;Required:boolean}>}>;views:Record<string,unknown>;actions:Record<string,unknown>;pages:Record<string,unknown>;renderTree?:Node}
export type Node={component:string;props?:Record<string,any>;children?:Node[]}
export async function api<T>(path:string,init?:RequestInit):Promise<T>{const headers=new Headers(init?.headers);if(init?.body)headers.set('Content-Type','application/json');const csrf=sessionStorage.getItem('bean_csrf');if(csrf)headers.set('X-CSRF-Token',csrf);const response=await fetch(path,{...init,headers});const body=await response.json().catch(()=>({}));if(!response.ok)throw new Error(body?.error?.message||response.statusText);return body as T}
