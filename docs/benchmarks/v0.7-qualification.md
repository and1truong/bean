# v0.7 Demo Factory qualification record

Date: 2026-09-01  
Bean: `0.7.0-alpha` candidate

## Deterministic product evidence

The maintained applicant-tracker definition contains 23 ordinary definitions. With seed `42`, it packages 76 records (4 jobs, 18 candidates, 24 notes, and 30 activities) and produces semantic source checksum `a190cd31201f304623065e3f3976aa273409d9a7721133d671641e80175592f9` and dataset checksum `9c019271a6db7e94e6c18f36ac9b5005149d4ecc9d6e2569ef4431c9774a46a0`.

`ats.spec.ts` proves the populated theme, metric, timeline, declared search, pipeline transition, and candidate detail. `package.spec.ts` copies the source to an isolated directory, packages it, deletes the source, starts the packaged executable, and reads the populated Views. Unit contracts cover all supported scalar types, deterministic IDs and relations, required-cycle rejection, exact replay, unsafe-target refusal, independent compilation of all ten patterns, package tamper detection, and failure-atomic destination preservation.

The two secondary definitions also validate and package with seed `42`:

| Application | Generated records | Source checksum | Dataset checksum |
| --- | ---: | --- | --- |
| CRM | 116 | `e5b3fab12414dd8026f59022243e2845ad9a9af56630c3b80e12ed25c3c8f7a8` | `ec6f072ecffbbbee17168e8c9e41b9e9c8a88a06ba2d4746193e8db3885738be` |
| Issue tracker | 81 | `85e2512e8b9016a1f0eb11dc2c06f887a69d97e0189101ede12dbdf1ab20e9ed` | `be2fb534cdddeea3605ab273985663955b13427a92a4d44d2f7b6b7f4e51b6c4` |

Executable, physical SQLite, and release IDs are recorded per package but are intentionally not semantic reproducibility claims: build bytes, release IDs, and audit timestamps can differ. `package verify` checks the exact physical artifacts named by its manifest.

## North Star status

This record is deterministic runtime and prepared-definition evidence. It is not reported as a prompt-to-demo agent timing run. The p50-under-five-minute and p90-under-ten-minute North Star requires repeated fresh external-agent runs using the frozen prompts and fields in [v0.7-demo-factory.md](v0.7-demo-factory.md). No such result is fabricated here; that final benchmark remains the only non-code qualification item before the v0.7 goal is marked complete.
