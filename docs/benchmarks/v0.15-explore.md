# v0.15 Explore onboarding qualification

Recorded 2026-09-03 on the local macOS qualification host with `bean 0.15.0-alpha`, SQLite, Chromium, and the deterministic ATS seed (`42`). The starting point was an already published ATS Entity model and seed data. No pre-authored View, Display, or Page created by the measured flows is counted as generation output.

Command:

```bash
cd e2e && bunx playwright test ats.spec.ts
```

| Run | Artifact authored through UI | Measured flow | Elapsed | Human interventions |
| --- | --- | --- | ---: | ---: |
| Cold | `candidate_follow_up`: filtered record View plus `/candidate-follow-up` table Page Display | sign in; select Entity; configure filter/route; preview; save; inspect validation + semantic-change result; Studio validate; publish; open result | 860 ms | 0 |
| Warm | `candidate_stage_snapshot`: grouped count View plus `/candidate-stage-snapshot` chart Page Display | select Entity; configure group/aggregate/route; preview; save; inspect validation + semantic-change result; Studio validate; publish; open chart | 653 ms | 0 |

The browser test asserts both complete paths stay below five minutes and verifies the published record table/chart, not merely API success. It also runs the pre-existing recruiting journey, but that time is outside the two measurements above.

These automation timings qualify the deterministic product path and catch runtime regressions; they are not a claim about human typing speed, external-agent p50/p90 performance, or data-model creation. A human or agent still has up to the product target of approximately five minutes to choose names and fields. CSV import, existing-table mapping, provider introspection, and Entity modelling are outside the measured v0.15 path.
